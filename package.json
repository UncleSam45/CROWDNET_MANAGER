{
  "name": "crowdnet-manager",
  "version": "0.1.0",
  "private": true,
  "description": "Private bridge-backed business development command center",
  "main": "main.js",
  "scripts": {
    "start": "electron main.js",
    "check": "node --check main.js && node --check preload.js && node --check browser-bridge.js && node --check app.js",
    "test": "python -m unittest -v"
  },
  "devDependencies": {
    "electron": "^37.2.3"
  }
}
